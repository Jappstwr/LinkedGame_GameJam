using UnityEngine;

public class renderTextureGrayStyle : MonoBehaviour
{

    void Start()
    {
        // TODO: remove this comment, the code would not compile with this line
        // rawImage.color = new Color(0.7f, 0.7f, 0.7f);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
